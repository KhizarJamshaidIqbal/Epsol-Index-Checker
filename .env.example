# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-strong-secret-min-32-characters

# Database (SQLite for dev, Postgres for prod)
DATABASE_URL=file:./dev.db

# For production with Postgres:
# DATABASE_URL=postgresql://user:password@localhost:5432/epsol

# Redis (optional, if using BullMQ). If missing, fallback to in-process queue:
REDIS_URL=redis://localhost:6379

# Encryption key for settings (32 bytes base64)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
EPSOL_KMS_KEY=replace-with-base64-32-byte-encryption-key

# Email provider for magic links (optional - configure for production)
# Example with SendGrid:
# EMAIL_SERVER=smtp://apikey:YOUR_SENDGRID_API_KEY@smtp.sendgrid.net:587
# EMAIL_FROM=noreply@yourdomain.com

# Or use nodemailer JSON:
# EMAIL_SERVER={"host":"smtp.gmail.com","port":587,"auth":{"user":"user@gmail.com","pass":"password"}}
# EMAIL_FROM=noreply@yourdomain.com
